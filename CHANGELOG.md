# 更新记录

## 2026-08-10｜信用卡关系、年费与日历功能升级

### 总览

- “银行额度分布”恢复显示完整银行名称，例如“招商银行”。
- 总授信额度和银行额度分布改为按额度组去重，避免主副卡或同银行共享额度的多张卡重复累加。
- 卡组织统计支持一张卡归属多个卡组织，圆环中心仍显示实际卡片数量。
- 当前年费成本按年费类型和完成状态计算：刚性年费完整计入；可减免年费完成后计为 0；期免和终免计为 0。

### 卡片管理

- 增加自定义排序模式，在卡片名称前提供拖动手柄。
- 支持鼠标与触屏上下拖动，顺序保存至浏览器本地存储。
- 搜索或筛选状态下拖动时，只调整当前可见卡片的相对顺序，不打乱隐藏卡片。
- 卡组织由单选改为下拉多选，支持银联、Visa、Mastercard、American Express、JCB 和其他。
- 表格同步显示主卡／副卡、独立／共享额度及额度组名称。
- CSV 导出增加卡片类型、额度方式、额度组、年费类型、减免状态、减免方法和期免有效期。

### 主卡、副卡与共享额度

- 新增卡片类型：主卡和副卡；每张卡独立保存持卡人，因此主副卡持卡人可以不同。
- 副卡可选择同银行主卡，保存时自动继承主卡的额度组。
- 如果被关联主卡原来是独立额度，系统会自动创建共享额度组并保留原额度。
- 主卡可使用独立额度、新建共享额度组、加入同银行已有额度组，或直接与同银行现有独立主卡建立共享额度。
- 同一额度组的名称和总额度会同步到所有成员卡。
- 删除有关联副卡的主卡时，副卡转为主卡并继续保留共享额度，避免额度关系丢失。

### 添加／编辑信用卡页面

- 表单重新设计为四个区域：基本信息、额度关系、账单与费用、权益与备注。
- 主副卡、额度方式、年费类型和减免状态采用统一的分段按钮。
- 不相关字段按选择动态隐藏，减少表单拥挤。
- 固定额度和共享总额度取消上下调节箭头，只允许输入非负整数。
- 卡组织使用与其他输入框一致的下拉多选样式。
- 弹窗标题和操作栏固定，长表单滚动时仍可随时保存或取消。
- 桌面端使用多列布局，移动端自动切换为单列或两列。

### 年费管理

- 移除“已减免金额”，不再要求记录部分减免金额。
- 年费类型扩展为四种：刚性年费、可减免、期免、终免。
- 可减免年费支持填写减免方法／条件，并记录“未完成／已完成”。
- 期免支持填写 `MM/YY` 格式的卡片有效期，连续输入四位数字时自动补充分隔符。
- 年费列表和卡片表格同步显示刚性、待减免、已完成、期免或终免状态。

### 还款日历

- 日历新增年费收取日，并使用紫色与账单日、还款日区分。
- 年费日期按月／日每年生成提醒。
- 同一天超过三条信息时显示“+N 展开”，点击可查看全部或重新收起。
- 右侧“本月提醒”与左侧日历在桌面端保持同高。
- 本月提醒每页显示 9 条，超过后通过底部分页按钮切换。
- 切换月份或返回本月时，自动回到第一页并收起已展开日期。

### 示例数据与隐私

- 删除内置案例卡片和“重置示例数据”功能。
- 页面首次打开时使用空卡片列表。
- 对已有浏览器数据执行一次兼容清理：删除 ID 以 `demo-` 开头的案例卡片，保留用户自己新增的卡片。
- README 明确说明仓库不包含卡片数据，真实备份继续由 `.gitignore` 排除。

### 数据兼容与备份

- 旧卡片自动迁移为“主卡＋独立额度”，不改变原额度。
- 旧版单卡组织字段自动迁移为多卡组织数组。
- 旧版年费减免金额仅用于迁移判断，新的数据不再保存该字段。
- JSON 备份版本升级至 v4，仍支持导入旧版卡片数组或旧版备份对象。

### 验证

- JavaScript 语法检查通过。
- Git 差异空白检查通过。
- 页面静态 ID 引用和重复 ID 检查通过。
- 主副卡共享额度、额度组去重及年度年费事件生成测试通过。
- 刚性、可减免、期免和终免四种年费成本规则测试通过。

