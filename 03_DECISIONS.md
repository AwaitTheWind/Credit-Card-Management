# 项目决策记录

## 2026-08-10｜创建项目

- 背景：用户要求新建“信用卡管理”项目。
- 可选方案：直接建立无编号目录；按工作区 SOP 建立标准项目目录。
- 最终决定：创建 `信用卡管理` 并采用标准目录结构。
- 决策原因：符合工作区统一项目管理规则。
- 后续影响：所有相关内容均存放在本项目目录，并由独立 Git 仓库管理。

## 2026-08-10｜本地应用技术方案

- 背景：用户希望创建一个可以在本机运行的信用卡管理软件，并可上传 GitHub。
- 可选方案：Electron/Tauri 桌面安装包；带后端数据库的 Web 系统；纯静态离线 Web 应用。
- 最终决定：首版采用纯 HTML/CSS/JavaScript 的离线 Web 应用。
- 决策原因：无需安装运行环境，Windows 可双击启动，跨平台，可直接部署 GitHub Pages；数据可留在浏览器本地。
- 隐私边界：代码中只提供匿名示例数据，不采集完整卡号、CVV、密码或验证码；真实 JSON/CSV 备份不进入 Git。
- 后续影响：如果需要系统通知、加密数据库或安装包，可在现有数据模型之上升级为 Tauri 桌面端。

## 2026-08-10｜共享额度数据模型

- 背景：主副卡可以由不同持卡人持有，同一持卡人在同银行的多张主卡也可能共享额度。
- 最终决定：不根据持卡人或银行名称自动推断共享关系，而是由用户明确选择独立额度或共享额度组。
- 计算规则：同一额度组在总授信和银行额度分布中只计算一次，卡片数量仍按实际卡片数统计。
- 兼容策略：旧数据默认迁移为主卡和独立额度；副卡关联独立主卡时自动建立共享额度组。

## 2026-08-10｜年费状态模型

- 背景：用户不需要记录部分减免金额，但需要区分刚性、条件减免、有效期内免和终身免。
- 最终决定：使用 `rigid`、`waivable`、`term_free`、`lifetime_free` 四种年费类型。
- 计算规则：刚性年费完整计入成本；可减免年费完成后计为 0；期免和终免计为 0。
- 期免格式：卡片有效期保存为 `MM/YY`，表单负责格式化和校验。

## 2026-08-10｜日历提醒展示

- 背景：同日多卡事件会挤压日历单元格，本月提醒数量也可能超过右侧区域高度。
- 最终决定：日历默认显示前三条事件并支持展开；右侧提醒每页 9 条并固定分页。
- 年费事件：年费日期按月／日逐年生成，与账单日和还款日一起进入日历和近期事项。
